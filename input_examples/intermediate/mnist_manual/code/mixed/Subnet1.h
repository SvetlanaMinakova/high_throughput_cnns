// File automatically generated by ESPAM

#ifndef Subnet1_H
#define Subnet1_H

#include "arm_compute/graph.h"
#include "support/ToolchainSupport.h"
#include "utils/CommonGraphOptions.h"
#include "utils/GraphUtils.h"
#include "utils/Utils.h"
#include <chrono>
#include <thread>

using namespace arm_compute::utils;
using namespace arm_compute::graph::frontend;
using namespace arm_compute::graph_utils;

class Subnet1 : public Example {
public:
  Subnet1(): cmd_parser(), common_opts(cmd_parser), common_params(), graph(0, "Subnet1") { };
  
  //DNN-dependent functions
  bool do_setup(int argc, char **argv) override;// DNN
  void do_run() override; //run DNN
  
  //DNN-dependent parameters
    int batchSize = 1;
    const char* INPUT_BLOB_NAME = "Convolution110";
    const char* OUTPUT_BLOB_NAME = "Convolution110";
    int INPUT_C = 8;
    int INPUT_H = 14;
    int INPUT_W = 14;
    
    int OUTPUT_C = 16;
    int OUTPUT_H = 14;
    int OUTPUT_W = 14;
    int OUTPUT_SIZE = OUTPUT_C * OUTPUT_H * OUTPUT_W;
 
 private:
   CommandLineParser  cmd_parser;
   CommonGraphOptions common_opts;
   CommonGraphParams  common_params;
   Stream             graph;
   
   //DNN-dependent functions
 
 };
 #endif // Subnet1_H
